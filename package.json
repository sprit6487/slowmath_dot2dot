{
  "name": "slowmath-dot2dot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  },
  "dependencies": {
    "@capacitor/core": "^7.2.0",
    "@capacitor/android": "^7.2.0"
  }
}
